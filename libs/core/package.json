{
  "name": "@team-orchestration/core",
  "version": "0.0.1",
  "description": "Pure TypeScript parser + join library for the team-orchestration mission folder convention. Reads missions/REGISTRY.md + missions/NNNN-*/state.md + (later) ~/.claude/projects/<hash>/*.jsonl transcripts. Angular-free. Runs in Node and browser.",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@team-orchestration/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^2.1.0"
  }
}
