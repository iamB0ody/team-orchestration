{
  "name": "@team-orchestration/cli",
  "version": "0.0.1",
  "description": "team-orchestration CLI. Prints mission registries + workspace summaries in hacker-theme terminal output. The dashboard's Angular/Electron frontend (later wave) wraps the same libs/core parser.",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "tot": "./src/index.ts"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@team-orchestration/core": "workspace:*",
    "@team-orchestration/shared-types": "workspace:*",
    "picocolors": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "~5.6.0",
    "vitest": "^2.1.0"
  }
}
